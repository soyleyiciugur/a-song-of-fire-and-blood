import { redirect } from "next/navigation"; import AuthForm from "../AuthForm"; import { login } from "../actions"; import { getCurrentUser } from "@/lib/auth";
export default async function Page(){if(await getCurrentUser())redirect("/forum");return <AuthForm title="Sign in" intro="Return to the tables and conversations of the realm." action={login} fields={[{name:"email",label:"Email",type:"email",autoComplete:"email"},{name:"password",label:"Password",type:"password",autoComplete:"current-password"}]} submit="Sign in" footer={{text:"New to the realm?",href:"/register",label:"Join"}}/>}
