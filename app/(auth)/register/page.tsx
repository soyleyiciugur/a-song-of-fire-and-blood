import { redirect } from "next/navigation"; import AuthForm from "../AuthForm"; import { register } from "../actions"; import { getCurrentUser } from "@/lib/auth";
export default async function Page(){if(await getCurrentUser())redirect("/forum");return <AuthForm title="Join the realm" intro="Create one identity for Taverns and Raven’s Eye." action={register} fields={[{name:"username",label:"Username",autoComplete:"username",minLength:3,maxLength:30,pattern:"[a-z0-9][a-z0-9_-]{2,29}"},{name:"displayName",label:"Display name",autoComplete:"name",maxLength:60},{name:"email",label:"Email",type:"email",autoComplete:"email"},{name:"password",label:"Password",type:"password",autoComplete:"new-password",minLength:8}]} submit="Create account" footer={{text:"Already a member?",href:"/login",label:"Sign in"}}/>}
