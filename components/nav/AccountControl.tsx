"use client";
import { useEffect,useState } from "react";import Link from "next/link";import { usePathname } from "next/navigation";import { createClient } from "@/lib/supabase/client";import type { Profile } from "@/lib/supabase/database.types";import { logout } from "@/app/(auth)/actions";import styles from "./navbar.module.css";
export default function AccountControl(){const [profile,setProfile]=useState<Profile|null>(null),[signedIn,setSignedIn]=useState(false),[loaded,setLoaded]=useState(false),pathname=usePathname();useEffect(()=>{const supabase=createClient();let active=true;const load=async()=>{const {data:{user}}=await supabase.auth.getUser();if(!active)return;setSignedIn(!!user);if(!user){setProfile(null);setLoaded(true);return}let {data}=await supabase.from("profiles").select("*").eq("id",user.id).maybeSingle();if(!data){const repaired=await supabase.rpc("ensure_own_profile");data=repaired.data}if(active){setProfile(data);setLoaded(true)}};void load();const {data}=supabase.auth.onAuthStateChange(()=>window.setTimeout(()=>void load(),0));return()=>{active=false;data.subscription.unsubscribe()}},[pathname]);if(!loaded)return null;if(!signedIn)return <span className={styles.authLinks}><Link href="/login">Sign in</Link><Link href="/register">Join</Link></span>;if(!profile)return <span className={styles.profilePending}>Profile loading…</span>;return <details className={styles.accountMenu}><summary aria-label="Account menu"><span>{profile.avatar_url?<img src={profile.avatar_url} alt=""/>:profile.display_name.slice(0,2).toUpperCase()}</span><b>@{profile.username}</b></summary><div><Link href={`/users/${profile.username}`}>Profile</Link><Link href="/settings">Settings</Link><form action={logout}><button>Sign out</button></form></div></details>}
