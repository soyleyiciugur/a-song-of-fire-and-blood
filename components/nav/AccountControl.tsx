"use client";
import { useEffect,useState } from "react";import Link from "next/link";import { createClient } from "@/lib/supabase/client";import type { Profile } from "@/lib/supabase/database.types";import { logout } from "@/app/(auth)/actions";import styles from "./navbar.module.css";
export default function AccountControl(){const [profile,setProfile]=useState<Profile|null>(null),[loaded,setLoaded]=useState(false);useEffect(()=>{const supabase=createClient();const load=async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){setProfile(null);setLoaded(true);return}const {data}=await supabase.from("profiles").select("*").eq("id",user.id).single();setProfile(data);setLoaded(true)};void load();const {data}=supabase.auth.onAuthStateChange(()=>void load());return()=>data.subscription.unsubscribe()},[]);if(!loaded)return null;if(!profile)return <span className={styles.authLinks}><Link href="/login">Sign in</Link><Link href="/register">Join</Link></span>;return <details className={styles.accountMenu}><summary aria-label="Account menu"><span>{profile.avatar_url?<img src={profile.avatar_url} alt=""/>:profile.display_name.slice(0,2).toUpperCase()}</span><b>@{profile.username}</b></summary><div><Link href={`/users/${profile.username}`}>Profile</Link><Link href="/settings">Settings</Link><form action={logout}><button>Sign out</button></form></div></details>}
