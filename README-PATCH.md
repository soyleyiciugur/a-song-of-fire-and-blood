# Missed fixes patch

Files address: NPC Favor writes, actual Taverns titles in Likes & Favor, no Raven's Eye flash when closing external lightboxes, and the styled Guestbook composer.

After copying files, run `npm.cmd run community:sync` so reaction-target migrations and the new RPC/target registry are applied to the linked Supabase project.
